services:
  app:
    container_name: reactivated-example-app-dev
    build:
      context: .
      args:
        UID: "${UID}"
    command: >
      bash -c "
      trap 'echo SIGTERM received, shutting down...; exit' SIGTERM;
      while true; do
        sleep 2;
      done
      "
    volumes:
      - /app/node_modules
      - /app/.venv
      - .:/app
    env_file:
      - ./.env
    ports:
      - ${DJANGO_PORT}:${DJANGO_PORT}
  psql:
    image: postgres:15.7
    container_name: reactivated-example-psql-dev
    user: "${UID}"
    restart: always
    # set shared memory limit when using docker-compose
    shm_size: 128mb
    env_file:
      - ./.env
    volumes:
      - ./.postgresql/data:/var/lib/postgresql/data
